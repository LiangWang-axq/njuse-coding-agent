# Copy to .env locally, or export these variables in the shell. Never commit real credentials.
AGENT_BASE_URL=https://api.openai.com/v1
AGENT_API_KEY=
AGENT_MODEL=gpt-4o-mini
AGENT_TIMEOUT_SECONDS=90
AGENT_MAX_STEPS=12
